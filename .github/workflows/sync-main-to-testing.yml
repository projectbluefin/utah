name: Sync main to testing

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  sync:
    uses: projectbluefin/actions/.github/workflows/reusable-sync-branches.yml@9c699818ddf1043b8f5af8b862f59f783c6c7bda # v1
    permissions:
      contents: write

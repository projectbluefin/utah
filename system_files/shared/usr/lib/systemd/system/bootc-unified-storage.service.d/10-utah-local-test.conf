# A bootc disk installed from localhost/utah cannot repull that ref from the
# guest's localhost registry. The normal published image has no utah.local
# kernel argument, so unified storage remains enabled there.
[Unit]
ConditionKernelCommandLine=!utah.local
